import Model.Wochentag;
import org.w3c.dom.ls.LSOutput;

public class EnumExample {

    static void main(String[] args) {
        Wochentag m = Wochentag.Mittwoch; // make instance of the constant Mittwoch
        System.out.println(m); // you can then print the value (OUTPUT: Mittwoch)


    }
}
